---
// Đảm bảo phần đầu file TRỐNG, không import Services hay Index
interface Props {
  title: string;
  description: string;
  image: string;
  link: string;
}
const { title, description, image, link } = Astro.props;
---
<div class="service-card">
  </div>
